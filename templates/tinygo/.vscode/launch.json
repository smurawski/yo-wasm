{
    "configurations": [
        {
            "name": "Debug WASM",
            "type": "lldb",
            "request": "launch",
            "program": "wasmtime",
            "args": ["run", "${workspaceFolder}/${workspaceRootFolderName}.wasm"],
            "cwd": "${workspaceFolder}",
            "sourceLanguages": ["go"],
            "preLaunchTask": "Make: Build TinyGo WASM"
      }
  ]
}
